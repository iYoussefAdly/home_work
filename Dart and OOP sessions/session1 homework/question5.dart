/*
var store a variable and it saves its type so we can't change it to another one
but dynamic we can change the variable type 
 */
void main() {
  var result = "2";
  // result = 2; //here's an error
  dynamic x = "6";
  x = 5;//here we can change the datatype without any error 
}
